# Supabase Connector Dashboard

أضيفت لوحة تشغيل عربية/إنجليزية إلى المشروع لعرض حالة اتصال Supabase والبيانات المقروءة فعليًا من العميل العام.

## ما الذي تختبره اللوحة؟

- الاتصال بمشروع `KING-DESIGNER` ذي المرجع `wxgekvzrqnnmacvsbibk`.
- قراءة جدول `profiles` بحد أقصى خمسة سجلات.
- عرض نتيجة الاستعلام الفعلية بصيغة JSON دون بيانات تجريبية.
- عرض حالة المخطط عندما يكون `public` فارغًا.
- توضيح تكامل GitHub وملفات migrations الموجودة في المستودع.

## التشغيل

```bash
npm install
npm run dev
```

ثم افتح الرابط الذي يعرضه Vite.

## التحقق

```bash
npm run typecheck
npm run build
```

## نتيجة الاختبار الحالي

تمكن الموصل من الوصول إلى مشروع Supabase، لكن مشروع `public` لا يحتوي جداول حاليًا بحسب اختبار MCP/SQL. لذلك تظهر اللوحة حالة اتصال ناجح مع تنبيه بأن البيانات غير متاحة حتى يتم تطبيق ملفات SQL الموجودة في جذر المستودع.

طبّق ملفات migrations بترتيب أرقامها في Supabase SQL Editor أو عبر مسار migrations المعتمد، ثم أعد الاختبار. تأكد من أن سياسات RLS تسمح بالقراءة المطلوبة للمستخدمين؛ المفتاح الموجود في الواجهة هو publishable/anon فقط، ولا ينبغي وضع service role key في المتصفح.

## قدرات Supabase باختصار

PostgreSQL، Auth، Row Level Security، Storage، Realtime، Edge Functions، وواجهات REST/JavaScript. يوفّر المشروع ملفات SQL تغطي الحسابات، المنشورات، القصص، الرسائل، الإشعارات، الخدمات، المحفظة، التحقق، والتقارير.
